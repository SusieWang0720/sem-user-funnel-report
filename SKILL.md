---
name: sem-user-funnel-report
description: 将神策/广告平台导出的 SEM 用户明细四张必传表（注册 registered、试用 trial、测试 tested、付费 paid）和可选创建表 created 清洗合并为 Google/Bing 分渠道漏斗明细报表。当用户提到用户明细合并去重、UIN 去重、SEM 漏斗报表、广告系列内容拆分（区域/关键词类/细分词类）、{campaign} 占位符修复、广告内容乱码修复、sitelink 清理、按 UIN 推断首次访问时间等任务时，务必使用本 skill。
---

# SEM 用户明细清洗 → Google/Bing 漏斗报表

把四张必传用户明细导出表和可选 created 表（同一批用户按漏斗阶段分别导出，互有重叠）清洗成一份
按投放渠道拆分、可直接用于分析的漏斗明细表。

## 输入

四张必传、可加一张 created 的结构相同 xlsx，核心列：
`UIN, 名称, 创建场景, 消耗场景, UIN类型, 流量来源, 首次访问时间, 广告系列名称,
广告系列内容, 广告系列字词, 浏览器语言, 广告系列来源, 注册时间, 创建/领取时间,
首次测试消耗时间, 首次付费时间`

## 输出

一个 xlsx，两个 sheet（Google / Bing），列结构：

`投放渠道 | UIN | 首次访问时间 | 广告系列名称 | 广告系列内容 | 区域 | 关键词类 | 细分词类 | 广告系列字词 | 浏览器语言 | 注册用户-神策 | 创建应用 | 消耗用户数-神策 | 付费用户-神策`

## 流程（两阶段，中间需要用户补数）

### 阶段一：合并 + 建表

运行 `scripts/build_report.py`：

```bash
python scripts/build_report.py --paid 付费表.xlsx --tested 测试表.xlsx \
  --trial 试用表.xlsx --created 创建表.xlsx --registered 注册表.xlsx --out 报表.xlsx
```

脚本做的事（如果手写代码也按这个顺序）：

1. **合并去重**：按 paid > tested > trial > created > registered 优先级拼接，created 未提供时跳过，
   按 UIN 去重保留第一条。付费用户的记录字段最全，所以优先级最高。
2. **筛渠道**：只保留 `广告系列来源` ∈ {google, bingmkt}。
3. **时间列转 0/1**：注册时间全部 =1（能出现在表里就是注册用户）；
   创建/领取、首次测试消耗、首次付费三列有值 =1、无值 =0，
   分别对应 创建应用 / 消耗用户数-神策 / 付费用户-神策。
4. **拆分广告系列内容**：格式为 `区域-Search-关键词类-细分-日期`，
   按 `-Search-` 切开，前半是区域，后半第一段是关键词类（通用词/品牌词/竞品词/场景词），
   第二段是细分词类（Chat/Call/Live/TRTC/竞品日期码等）。
   不要用简单 `split('-')` 按下标取——存在 `巴西--Search-...` 双横线的脏数据。
5. **分 sheet**：google → Google sheet（投放渠道=谷歌），bingmkt → Bing（必应），
   按首次访问时间升序排。
6. **导出缺失清单**：广告系列内容为空、为 `{campaign}` 或非标准格式的行，
   UIN 写入 `missing_uins.csv`。

### ⏸ 暂停：请用户补数

阶段一结束后**必须停下**，把 `missing_uins.csv` 给用户，告诉用户：
「这些 UIN 的广告系列内容缺失，请去数据平台（如 impala/神策）按 UIN 重新查一次
utm 字段再给我」。不要跳过这一步自行猜测，也不要静默留空。

用户返回的补充表典型列名：
`user_id, first_utm_campaign, first_utm_content, first_utm_term`
（user_id 即 UIN；注意引荐流量的行 utm 为空，属正常，跳过即可）。

### 阶段二：回填 + 修复

运行 `scripts/fill_supplement.py`：

```bash
python scripts/fill_supplement.py --report 报表.xlsx --supplement 补充表.xlsx
```

按顺序执行五类修复（每一步都要在最后重新解析 区域/关键词类/细分词类 三列）：

1. **补充回填**：按 UIN 匹配，把 utm_campaign/content/term 填进
   广告系列名称/内容/字词（只填空行，不覆盖已有值）。
2. **{campaign} 占位符**：内容为 `{campaign}` 的行，用相同「广告系列名称」
   （数字 ID）的其他正常行的内容（取众数）替换。
3. **删 sitelink**：广告系列字词含 sitelink 的行是附加链接点击、无有效内容，整行删除。
4. **乱码修复**：内容含 `�` 的行，优先用相同广告系列 ID 的正常内容替换；
   匹配不到再用规则修（如 `通�词→通用词`、`�牌词→品牌词`）。
5. **缺失时间推断**：UIN 是按时间递增发放的。缺首次访问时间的行，
   在**同前缀系列内**（如 4502 开头和 2000 开头是两套编号，不能混）找 UIN 数值
   最接近且有时间的行，取其时间。最后全表按 时间+UIN 重排。

## 格式要求

两个脚本都会套用统一样式，手工修改后也要保持：深色表头（#1A1A1A 底、白色加粗）、
Arial、全部居中、首行冻结、自动筛选、首次访问时间格式 `yyyy/m/d`（只留日期）。

## 注意事项

- 每次用 pandas 重写 xlsx 都会丢样式，写完必须重新套样式（脚本已内置）。
- 用户可能中途重命名文件，操作前先 `ls` 确认当前文件名。
- 文件在 Excel 中打开时（目录下有 `~$` 锁文件），保存后提醒用户重新打开。
- 汇总每一步的行数变化并报给用户（合并前后、删除多少、修复多少）。
